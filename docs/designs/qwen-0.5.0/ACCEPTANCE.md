# Qwen installed acceptance, 2026-09-10

One real permanent umka installation and real home/config/login/service.
Current installed combined source 68e04075155a0072c6121a84a4a4f5bbc5d4e175;
binary 9b05b0fc64850086f605d97011451311bc3927c940df69939fe6652dce57428c,
3,891,362 bytes. Earlier b047 lane binary 8b740be8... is separate evidence,
not a protected installation. [EVIDENCE.json](EVIDENCE.json) binds all seals.

I = external `installed-interactive-68e0407`, P = `installed-b047ce0`,
L = `installed-b047-lifecycle`, under `/home/antst/qwen-architecture-20260910`.
I raw installed records are under remote/. Native 0.23.0 executed the public,
parent and lane model rows. Qwen's own updater installed 0.23.2 on first quit;
subsequent resume/ordinary/TERM/abrupt rows bind that version. Those later
zero-model rows are not fresh 0.23.2 public-model-tool credit. No native patch,
permission/discovery override or alternate configuration was used.

| Requirement | Actual evidence and limit |
| --- | --- |
| One permanent binary/private alias/one skill | I BUILD and install/final-installed records; all 13 archive members and installed binary match. Native extension and permanent payload have one generic skill and no global mcp.json. Controlled literal installer twice also removes stale skills/MCP manifests and preserves unrelated sentinels. |
| Fresh native identity/name/groups | I fresh-zero process/registry/native/input/controller. Native 1b134f76... matches helper env/initial event/registry. Actual `/rename -- --auto`, manual title --auto, mixed qwen68,a,b,c,d. Native slash record says sentToModel:false. |
| Actual native public tool | I parent-native-final lines 8/10 ToolSearch call_393fe4f496bb464a83619afe; 12/14 Sessionbus call_e87e1df479cf4b93bc54ed53 list returns own ID/title/groups; 16 exact marker. Initialization alone is not the evidence. |
| Parent owns child lifecycle/pointer | I native spawn 21/23, start 25/27: bd4899e2... /run-e1fhamwrs6z0/1 owned by initial parent. Ordinary completion pointer 30 follows parent STARTED 29. Native status 32/35 and37/39 return identical completed/end_turn/QWEN68_CHILD_DONE, matching separate native child answer. Ack 41/43, close 45/47, final COLLECTED 49. No shell/delegation substitute. |
| Inbound write vs observation | I inbound-controller written message-cbk5wgceqw7g/delivery-jftmlnd25izw; native envelope 50 and exact QWEN68_INBOUND_OBSERVED 54. Input file has one rename/pointer/message. Receipt proves append only; native response independently proves observation. |
| Native title resume/new resources | I resume launch/process/input/registry/controller: native --resume=--auto selects original ID in a new process/runtime; raw two-space name becomes native manual two words. Old transcript display is not replayed-model credit. |
| Ordinary skill-only activation | I ordinary process trees, native /mcp: zero servers and /skills one Sessionbus extension plus 14 bundled skills; no own helper or peer. No model request or skill toggle. Native quit exit 0. |
| Normal quit/TERM | I resume normal quit, later pane exit 0/process/runtime absence. TERM validates exact launcher PID/start/binary; all five PIDs/runtime absent and row withdrawn, later pane exit 1 retained. |
| Native updater restart | I first quit remains live because updater relaunches 0.23.2 under original launcher. Old integration withdraws; replacement hasone MCP server offline and no new owner. Explicit second quit is operator cleanup, not normal-first-quit evidence. Fresh launch/resume is supported; no rebind/retry mechanism. |
| Abrupt launcher loss | I exact SIGKILL then peer withdrawal and all five PIDs absent in this PTY case; stale empty claim/input/FIFO directory remains. Operator removal and later absence are separate. No killed-launcher cleanup/reaping claim. |
| ACP new/resume/helper identity | Earlier3fba phase1 zero-input new/resume/close; P resumed public composition binds native new/load ID and unique per-session endpoint. Helper bootstrap env is not ACP identity. Controlled startup gate/EOF/failed Open/cleanup tests remain separate. |
| Native lane public tool | P native ToolSearch→one Sessionbus list and exact full three-chunk answer vs repeated cursor, ack/close, later seven-PID absence. Original no-call harness forbade ToolSearch; corrected pre-parser attempt failed unavailable. Both failures remain retained. |
| Unsent stage/explicit run | L stage-seed records: queued marker absent from pre-run native snapshot; explicit request omits it, actual result includes it once; next run no replay. Native/public answers and repeated cursor/ack match. |
| Seeded wake/following run | L two runs in one generation, seed receipt written after request write; native complete answers match cursors/acks. No injected/native-admission credit from write alone. |
| Native pull/interrupt/healthy next | L active-interrupt: native pull yields written, same run interrupted/cancelled, following /2 completes. Final native history has one mid_turn_user_message before next explicit user. Recording is proved; model consumption before cancellation is not. No raw ACP frame capture claim. |
| Cursor/cleanup | P/L repeated reads/acks, close and later identified 7/19-PID and endpoint absence. I actual parent status/ack/close supplements package composition. Result retention ends with Worker lifetime. |
| Permissions/policies | Caller/native settings preserved. I child returns explicit effective persistent=false/auto_close_ms=0/idle stage/notifytrue. Independent shared policy matrix and refusal/cancellation tests are controlled; no new four-policy native deadline or all-permission-modes PASS is claimed. |
| Native selectors/switch limits | Fresh native title resume is installed. Bare/ID/continue/fork/error argv preservation is controlled-tested; no new installed matrix credit for every selector. In-process outbound/inbound switch attribution is the explicitly accepted limitation, not an enforced switch ban or repaired capability. |

The parser correction b047 discriminates session/update envelopes before decoding
owned assistant text; native tool-call content arrays remain valid. Original
installed malformed-update failure is preserved, while its exact unseen frame
is not invented. Controlled native-shaped old-fail/new-pass tests establish the
bug/fix; P's successful corrected public call establishes actual composition.

Full actual Worker/ACP tests cover staged pre-write refusal, seed ReportDelivery,
native batches of ten/capacity/no-pull/late recovery without replay, cancel/terminal
both orders, joined cancel and healthy next, bounded duplex work/output,
child-exit buffered terminal, startup adoption/cleanup and cursor behavior.
Compiled aliases cover inherited stdin EOF/TERM, undrained output TERM and
pending Caller EOF. Interactive tests cover claim races, fresh-title confirmation,
blank live title, FIFO open order/bounded drain/close+join and OS ownership.

Reviewer counterexamples are retained: literal --auto naming, native whitespace
normalization/title limit, recording-disable and camel aliases, and native config
empty/comment semantics. Native tools/settings were not changed to pass tests.
Linux race/vet/lint and Darwin compile/lint have their exact recorded scope;
final GitHub CI is a separate merge gate. No native macOS execution is claimed.

I retains native background/dream API/tool telemetry after the visible inbound
result. These are not erased or counted as requested acceptance turns. First
quit/updater relaunch and the harness Python module-name collision are preserved
separately from product successes. Native-owned history/background behavior does
not constitute wrapper persistence. No current Codex/Claude implementation or
shared MCP engine change is included in this Qwen unit.
