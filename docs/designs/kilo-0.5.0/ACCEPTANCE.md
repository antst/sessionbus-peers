# Kilo installed acceptance

Installed Linux acceptance is complete within the scopes and limits below.
Evidence is retained under
`/home/antst/kilocode-architecture-20260911`; each packet binds its own source,
artifact, native version, first outcome and cleanup. The installed runtime is
the b71 snapshot measured in SIZE, using Kilo 7.6.2 and OpenCode 1.18.30 with
their bundled Bun 1.3.14. No separate target prefix or config was used.

| Evidence packet | Established scope |
| --- | --- |
| `implementation-dev1/combined-900`; `installed-900-dev1` | Five complete permanent installs, executable/payload correspondence, the source-rendered Codex MCP entry, config preservation and actual bundled-runtime transport/kit checks. |
| `native-zero-dev1` | Zero-model lane new/close/exact resume, ordinary daemon-backed versus managed embedded Kilo on one directory, managed-only tool inventory, native-only quiet resume with `-s`, title/groups and normal double-Ctrl-C exit with cleanup. |
| `native-term-dev1` | One initialized actual native serve process, one TERM, direct parent wait 0 and PID absence. This is separate from Worker close-RPC semantics. |
| `implementation-dev1/skill-47f`; `installed-skill-47f-dev1` | Two permanent archive installs and native owned skill paths; actual ordinary Kilo discovery matches the permanent skill body/path/description, with no ordinary Sessionbus tool. |
| `oc-skill-concurrent-installed-dev1` | Actual ordinary OpenCode health, exact permanent skill discovery and tool absence with concurrent PTY pumping. Earlier HTTP timeout remains separately recorded, without a reconstructed cause. |
| `implementation-dev1/readiness-b71`; `installed-readiness-b71-dev1` | Only readiness/TUI content changes from 47f, unchanged binaries/kit, exact permanent member correspondence. First implementation-probe stall is retained separately. |
| `broadcast-installed-dev1`; `readiness-corrected-installed-dev1` | Both actual bundled runtimes: BroadcastChannel capability and the installed readiness implementation's six rows, eight natural Worker exits and natural main exit per product. Cancellation receipt and actual joins are recorded. |
| `service-provider-env-dev1` | Ignored quoted systemd EnvironmentFile corrected, existing private service environment is extended on the host with only the selected login provider key, idle daemon restart and presence-only verification. Hub generation and unrelated config remain unchanged. |
| `lane-tool-47f-dev1` | Original HTTP 500 is bound to the exact native ProviderModelNotFoundError and user-only history; no provider invocation or credential success is claimed. |
| `lane-tool-service-context-dev1` | After the concrete service repair, the same explicit model completes an actual Sessionbus list/self_info call; native/public results match, repeated status matches, acknowledgment/close succeed and owned generations/socket disappear. |
| `lane-lifecycle-b71-dev1` | Native asks permission for exact `sleep 360`; unattended rejection is observed. No sleep or long-silence result. Controller cancellation closes its connection; the cleanup attempt reports `sessionbus connection closed`, with no successful explicit close RPC. Captured Worker/native generations and socket disappear through nonpersistent owner-loss cleanup. |
| `acceptance-agent-installed-dev1`; `lane-lifecycle-agent-b71-dev1` | A temporary native primary agent allows only exact `sleep 360` and `sleep 45` bash commands over an explicit ask default. Native policy projection is checked before the run and the original config is restored afterward. The actual command lasts 322.731 seconds, public status remains running before interruption, and native abort/no-output records agree with the terminal public result. Repeated interrupted status, active input staged only into the next USER, healthy next turn, exact resume seed and retained agent selection are verified; all seven captured generations and both sockets disappear. |
| `managed-controls-installed-dev1` | Native title propagation, zero-message fork selection, original Peer retention and selected fork deletion/home toast. HUP returns launcher 0 rather than fixture-expected 143 (exit reason unattributed); later exact generations, directory and same-group Peer are absent, with original history retained. The assertion mismatch remains recorded. A separate launcher KILL returns -9 with captured native/Peer absence; its stale directory is recorded and operator-removed, without wrapper cleanup or native exit-cause credit. |
| `interactive-question-installed-dev1` | First context fixture reaches an actual pending native question, then stops on its optional `blocking` assertion before switching, sending or replying. The raw GET object was not retained before that assertion, so its original field value is unknown. Native question waiting does not depend on this UI field. TERM containment joins the launcher and the captured native generation and private directory disappear; no context/delivery claim follows from this attempt. |
| `interactive-question-pending-installed-dev1` | Corrected fixture records the pending request (`blocking: null` in this attempt), its running question call and owned USER. After the UI switches, busy delivery to the old session is queued and remains unsent until the ordinary user replies. The old tool retains its own identity; a foreground native general Task reports its actual child identity, parent and resolved DeepSeek model. Busy and idle deliveries each become one native USER matching the real receipt, then receive completed acknowledgments. Normal double-Ctrl-C exit returns 0 and all three Peers, native generation and private directory disappear. |
| `parent-composition-installed-dev1` | Native parent uses supported `run`, then `wait`, `status`, `ack` and `close` for its one spawned bus child. The full collected results match each other and the child's actual native answer, and the completion pointer becomes a new parent USER with its own completed acknowledgment. The fixture recognized only `start`, so its monitor did not finish; that failure is preserved. Owned fixture interruption runs TERM containment, with four captured generations, lane socket and parent directory absent. No extra model input or fixture-performed child close repairs the sequence; this row claims neither literal `start` nor normal key exit. |
| `opencode-regression-installed-dev1` | The current service-context instance exposes the selected DeepSeek model. After one prompt, an installed native skill hook adds a synthetic evaluation USER; the fixture stops on its one-new-USER assumption before a tool result. Both the submitted prompt and synthetic message are retained. TERM containment joins the launcher, captured generations and private directory disappear, and no normal key exit is claimed. |
| `opencode-regression-skill-installed-dev1`; `oc-skill-context-provenance-dev1`; `oc-skill-catalog-dev1` | The next fixture also stops before a tool: its literal evaluation text excludes other suggestions selected for the amended prompt. Native hook source and read-only discovery establish the variable formatter and 48 installed skill summaries, with at most five matches. The prompt and synthetic messages, containment and both failed assertions remain recorded. Catalog discovery uses the existing bundled runtime without dependency installation or a native model request. |
| `opencode-regression-catalog-installed-dev1` | Final same-installation OpenCode regression uses the actual service environment and active DeepSeek model. One actual Sessionbus list call has the admitted native session identity; its complete output equals the final native answer. The known skill-evaluation USER is correlated through the installed catalog and native assistant ancestry; no use_skill call occurs. Native idle is observed before one Ctrl-C, normal exit is 0, and all captured descendant generations, Peer and private directory disappear without fixture TERM. |

The ordinary discovery fixtures' later key-exit observations did not establish
normal quit; failure containment is recorded separately. Those outcomes do not
erase the earlier managed quiet-resume normal-exit proof. Native history was
retained except where a row explicitly owns and deletes a fresh empty fork.

The original readiness probe's Worker awaited readiness during module evaluation,
preventing Bun 1.3.14 from delivering the queued parent abort command. The
corrected fixture retains an owned completion task without pending module-level
await, records abort receipt and joins actual Worker exits. That fixture defect
is distinct from the real macOS filesystem-watcher startup flaw fixed in b71.
Linux bundled-runtime proof and macOS CI are not native Darwin acceptance.

The model lookup failure is attributed only to its own native error and the
captured daemon/login environment difference. The systemd installer correction
is bus PR54 at 55c354a; unrelated historical OpenCode model failures have no
retroactive cause assigned. No credential values or hashes leave the host.

Some controller runs have outer SSH status 1 despite successful inner controller
and observer results. These statuses remain separate in their packets. No model
run is repeated merely to erase an outer harness status.

The long lifecycle has an initial live empty-output snapshot at 50.079 seconds
and final native timestamps covering 322.731 seconds. The intended later live
snapshot was not captured. The synchronous HTTP request behavior is source-bound;
this packet does not capture individual response bytes on the wire. Native
permission rejection and the separately configured acceptance run retain their
different policy conditions.

## Release binding

The installed observations retain their actual b71 runtime and native executable
bindings. Later documentation-only commits carry those results by source equality;
they do not relabel the tested binaries as new published artifacts. Release
`SOURCE.txt`, checksums, binary build metadata and permanent member/config
correspondence are checked separately. SIZE remains the explicitly measured b71
snapshot, including both physical installations.

Native Darwin acceptance remains deferred across this campaign; macOS source/CI
coverage and Linux installed runtime evidence retain their separate scopes.

The separate Grok post-terminal receipt policy question remains recorded in its
delivery amendment. It is not silently changed by this Kilo acceptance campaign.
