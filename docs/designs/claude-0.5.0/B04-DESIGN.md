# B04 — stamped interactive MCP owner with first-report publication

**Current contract:** [FIRST-REPORT-CONTRACT](FIRST-REPORT-CONTRACT.md), stamped by delivery-5ee640db9140e1d13a3f2dc9ebec402e, replaces FP-C01. No launch, first-prompt or first-turn deadline. The interactive implementation inventory is now [submitted for stamp](INTERACTIVE-IMPLEMENTATION-INVENTORY.md); no implementation release is inferred.

Interactive uses a native-exec launcher and product-kept stdio MCP owner with one bus connection/Caller and in-process public tools. The new inventory selects the published kit Connection/Caller exports directly; its retrying Peer is not selected. This boundary amendment awaits the inventory stamp. Identity arrives only through native mcp_tool hooks: UserPromptSubmit or a usable Stop may establish presence; SessionEnd withdraws matching presence and never creates it. No interactive SessionStart subscription, retained parent, private endpoint, capability, PID binding, command hook or tool forwarder. A session without a received usable report is absent/unaddressable. Actual native name is optional under BN01; template-empty fields cannot erase a name.

The hidden native report handler is omitted from the model-visible tools list. FP01 proves one Stop invocation of that hidden handler; source supports the direct native call path. N/U/D/L/X now establish the scoped interactive identity, delivery, nested and lifetime observations described in the inventory. They do not establish universal provenance/context, lane configuration, published-owner EOF or production-server validation. [FP01 review](../../evidence/fp01-final-review/REVIEW.md) retains the historical first-prompt failure and separate Stop success.

Native arguments, naming and resume remain product-owned. Groups come from launch metadata. Same native ID follows daemon connection/supersession rules without locks or invented suffixes. Deliberate identity withdrawal is distinct from unexpected connection loss; the latter ends this owner integration under the specified Connection lifecycle. The pinned kit Peer schedules retries and cannot be credited with this policy; no SDK override is used. No adapter retry/replay or report cache.

Lane remains token-selected through the same launcher: daemon-owned retained worker, SDK-controlled child, initial command-hook report, private owner endpoint and stdio tool forwarder. No extra peer connection from the helper. [FP06 composition](FP06-COMPOSITION-PROPOSAL.md) is stamped: the common installed mcp_tool UserPromptSubmit/Stop/SessionEnd reports execute in the interactive owner or forward through the existing lane endpoint to the sole worker. Lane adds only SessionStart as a command report; no duplicate command report for the shared events. Launch-token dispatch selects owner versus forwarder mode. Its four runtime preconditions remain open under OB03, without deleting the lane initial-report path. L01 is verified only at its declared artifact/source metadata strength; L02/revised OB03 remain unproved and unreleased here.

BD01 written, BN01 optional names, BC01 interrupt semantics and separate C8 bus-kit requirements remain. [B06](B06-PLAN.md) gives current released gates. The exact interactive-only inventory is submitted; the lane inventory and whole-product implementation release remain withheld. Prior candidate stamps/failed rows remain historical evidence in revisions; none is retroactively green.

## Native version changes

FP05 partial4bf03ab directly records a265 root and266 nested child after the installed entry changes during the root lifetime. Integration must tolerate this product-owned update behavior and never select/pin a native version. Hash guards in evidence probes qualify observations only and are not production behavior. The42-slice [266 source rebind](../../sources/native-2.1.266-rebind/REPORT.md) preserves scoped design dependencies;265 runtime results are not promoted to266 runtime facts.
