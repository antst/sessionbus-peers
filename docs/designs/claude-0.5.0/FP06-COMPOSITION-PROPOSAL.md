# FP06 additive hooks — stamped composition boundary

**STAMPED by delivery-0e63eaf3447786179b67c9b44425cc9b.** This selects the boundary, not runtime acceptance or production implementation. Dev1 continues FP03 → FP04 → FP05; the four runtime preconditions below are folded into OB03 when lane rows are released. The exact source review rejects the assumption that adding lane settings suppresses a static installed MCP hook set. The selected boundary composes the events through the already required owners/forwarder.

| Element | Interactive | Lane | Invariant / earlier comparison |
|---|---|---|---|
| Installed mcp_tool UserPromptSubmit/Stop/SessionEnd | Hidden handler executes in product-kept MCP owner. | The same hidden handler forwards the native report through the existing lane tool endpoint to the worker. It never publishes a peer itself. | One owner of presence per mode. FP01 proves one interactive hidden Stop call, not lane report delivery. R3/R4 Claude had no hooks; no historical mode-composition pass is claimed. |
| Lane initial command-hook report | Not installed for interactive. | Lane's existing native launch configuration supplies SessionStart to the already required worker endpoint before native open commits. No command-hook duplicate for the same prompt/Stop/End events is added. | Native pre-input identity required by lane session.open; NP02 supplies source/runtime lead, full lane composition still must be proved. |
| MCP process | Owns Peer/Caller and public tools. | Existing stdio forwarder carries public tool calls and the hidden native report to the same worker endpoint. | No additional process, connection, endpoint, report buffer or retry. This extends the existing lane forwarder's declared requests; it is not claimed to exist already. |
| Identity/lifetime | Stamped first-report contract. | Worker remains authoritative and open is not delayed until prompt/Stop. Native child completion/worker lifetime and identity changes retain lane-specific obligations. | No transfer of interactive optional publication timing into lane open, and no extra lane peer hello. |

The launch-token mode dispatch already required for the public launcher determines MCP owner versus lane forwarder behavior. That is integration mode ownership, not a reimplementation of native product behavior. The hook lists themselves are deliberately common; no hook is silently ignored to simulate native exclusion.

The following four FP06 runtime preconditions remain open under OB03: (1) the existing native lane hook/config entry can supply only the required SessionStart command report while retaining native caller flags/settings; (2) native lane mcp_tool reports reach the configured forwarder without startup errors on its required event paths; (3) forwarding preserves native identity/report fields and one worker authority, with hidden handler absent from the model-visible catalog; (4) no duplicate report authority, extra peer or user-requested turn is introduced. Native child termination and ID replacement remain real lifecycle tests, not results inferred from source composition.

If those native surfaces do not support this composition, leave the lane row blocked and reshape it explicitly. Do not add a lane report-discard handler, duplicate command/MCP handling for the same event, a wrapper settings merger, private queue, retry or fallback. The source finding alone does not authorize a new native experiment or bypass the released FP sequence.

Exact production inventory remains withheld pending the required proof and implementation release. This stamped boundary changes no wire contract or process count and does not weaken lane open or communication requirements. Folding its preconditions into OB03 does not release OB03 or L02 here.
