# Held lane guidance

These twelve files are moved byte-for-byte from the four former top-level
Claude lane skills at reviewed commit356a134. They describe a legacy tool
surface and are retained only for future lane design and historical review.
They are outside the active `claude/` plugin root and npm package. Do not load
or install these skills as current candidate guidance. Moving them is not a
source deletion saving; whole-size accounting includes them separately.
