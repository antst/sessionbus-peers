# OpenCode acceptance index

The selected rewrite acceptance rows are complete at the limits below.
Source/controlled reviews and installed native outcomes remain separate.
External packet root:
`/home/antst/opencode-architecture-20260910`.

| Evidence packet | Exact scope |
| --- | --- |
| implementation/dev1-kit-0b35c99; installed-ee7381a | ee7381a package, permanent installation, pinned kit and executable/member bindings. |
| installed-7f762ff; installed-1e12254 | Native 1.18.29/Bun 1.3.14 watcher/basic kit and buffered private transport tests. Original omitted-callback failures precede the reviewed actual-close correction; no claim that Bun delivered omitted callbacks. |
| installed-ee7381a/BUFFERED-KIT-REPORT.md | Four actual bundled-runtime SDK/OwnedPeer buffered-close rows; 32 operations settled with omitted callbacks recorded, after the generic kit correction. |
| installed-ee7381a/ZERO-FIRST-REPORT.md | Wrong controller product token spawned raw native instead of the Worker. Failure and native auto-update from 1.18.29 to 1.18.30 retained; no lane/model success. |
| installed-ee7381a-zero-corrected | Corrected opencode-peer controller: actual new/close/exact resume/close, zero native messages, four identified process generations and two endpoint paths absent later. Source-bound Open readiness, not raw native HTTP-frame capture. |
| installed-ee7381a-interactive-phase1 | Native 1.18.30 native-only quiet resume, actual host Solid/name/groups/Peer, zero messages before prompt; ordinary no-own-tool native registry and normal exit; one actual interactive generic list/result matching native self_info and full final answer. |
| installed-ee7381a-parent-first | Actual parent spawn/start and incoming unavailable completion pointer; status/status/ack/close returned not_connected. Child has stored user input but no assistant record. No successful collection claim. |
| implementation/dev1-combined-502d2b1; installed-502d2b1 | Signed combined source, full local race/vet/lint/build/58 JS tests, exact Linux/macOS all-eight green CI, archive and permanent-member equality. The retained interactive JS/kit equals ee; file refresh did not reload that live native context. Fresh lanes and later quiet lifetime resumes used the corrected Go binary. |
| model-availability-ee | Current existing TUI provider/agent inventory only: configured OpenAI model absent from that instance, selected Google model present. Not the former child's exact error, credentials proof or a config repair. |
| installed-502d2b1-parent | Explicit Google model: actual parent spawn/start, native USER completion pointer, two equal complete status reads, ack and close; child native answer equals public answer. Five captured generations and endpoint absent later. |
| installed-502d2b1-native-tool | Actual lane generic list call/result with native self_info, complete native/public answer equality, repeat read/ack/close and five captured generations/endpoint absence. |
| installed-502d2b1-lifecycle | Idle stage remains unsent until explicit Run; native bash sleep with timeout 400000 remains running with empty output at 310.139697 s, native lifetime 320.973 s before abort. Active noReply input written once, interrupt, healthy next Run, close/exact resume and seeded wake with full native/public answer equality. Eleven captured generations/two endpoints absent later. |
| installed-502d2b1-tui-controls | Actual native selection, blank title omitting bus name, zero-message fork and navigation among three retained owners. Native API success is corroborated by panes and Peers; route metadata alone is not per-call identity. |
| installed-502d2b1-interactive-context | While old native bash was running, TUI switched to another established session; later old-session list retained old self_info. Native general Task child's own list identifies its native child Peer. Busy receipt queued_for_next_turn drains once after idle; separate idle receipt written; both native USER inputs have completed acknowledgments bound by parentID. |
| installed-502d2b1-lifetimes | Selected empty fork deletion, home route and only its Peer withdrawn; normal quit and fresh TERM/HUP quiet resumes remove owned resources and withdraw Peers. KILL in this PTY also left captured native processes absent, but its stale directory required separate operator removal. Original 32/blank 0/child 4-message histories retained; owner test2/qtest panes live. |

The phase1 packet also preserves the nonlogin tmux-PATH fixture failure and the
cross-kind resume refusal. A durable lane's native ID cannot become a Peer even
after close. The corrected quiet fixture used one native POST /session with
empty input, captured zero messages, then resumed that native-only ID through a
fresh managed launcher. No alias, lane forget or history reclassification was
used. Native 1.18.30 observations are not attributed to the earlier 1.18.29 binary.

The parent failure's exact original HTTP/SSE error was not captured. A native
configured-model difference and independently reproduced native-summary parser
defects must not be presented as proof of that unseen frame. Original failure,
controlled before/after tests and any corrected installed composition retain
their own source/version bindings.

The original default-model failure remains distinct from the successful explicit
`open.model: "google/gemini-3.1-pro-preview"` requests. Native agent, permissions
and settings were preserved. The wrapper does not inherit the displayed TUI
model implicitly or silently replace native defaults. Summary parsing and cwd
fixes have controlled old-fail/new-pass evidence; the original missing frame
still has no reconstructed cause.

Two long controller harnesses returned outer SSH status 1 although their retained
controller and observer exits were 0 and native/public assertions passed. The
exact harness text and this discrepancy are retained. Independent empty login
and nonlogin shell commands returned 0; they do not explain those earlier SSH
statuses. No product/model rows were repeated to erase this discrepancy.

A running native tool snapshot and native start/end timestamps establish the
long-operation row, not the controller's 330-second delay. Native abort output
and MessageAbortedError establish interruption, not successful sleep completion.
A written receipt is native handoff, not guaranteed same-turn consumption.
Cleanup proves later absence of the captured process generations and paths,
not every possible descendant or whole-host containment. KILL's native exit
cause is not independently attested; the killed launcher gets no child-join or
stale-directory cleanup credit. Only the selected empty fork was deleted.

[SIZE.json](SIZE.json) and [the measurement script](measure.py) bind source,
selected dependencies, archive and permanent inventory. Documentation/accounting
commits do not change the reviewed 502 runtime; the record names its measured
source and artifact revisions rather than implying a self-referential hash.
Final exact-head CI and merge/release checks are recorded with the PR separately.

The accepted SIGKILL containment limit and ordinary skill visibility remain
documented in the product README. Existing owner test2/qtest sessions are not
acceptance fixtures and remain available. No native patch, alternate target
runtime/prefix, permission grant or repeated model campaign is part of the
accepted scope.
