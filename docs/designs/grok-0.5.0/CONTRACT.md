# Grok implementation contract

Owner release: 2026-09-10, “go” and “make proper grok implementation”. Baseline peers eb727701; source/evidence inventory in ../archaeology/REPORT.md. This contract releases the invariants and uncontested work below. The named native-composition questions below are now selected by ACTIVATION-AMENDMENT.md, NAME-AMENDMENT.md and DELIVERY-AMENDMENT.md. Their exact runtime composition remains subject to the specified installed acceptance.

## Required outcome

One small Go product artifact serves interactive launcher, daemon lane worker and private MCP aliases. Reuse existing native Grok facts and bug fixes; do not copy Claude/Codex process topology blindly. No Node dependency, hidden second Caller, native session resolver, regenerated peer identity, receipt journal, ancestry registry or timer-based replay. Only the generic Sessionbus skill/tool is installed.

UMKA IS DISPOSABLE AND DEDICATED. Iteration replaces the real permanent Grok installation, real home/config/login/PATH/service. Dev1 owns implementation and host writes; dev2 may perform the explicitly coordinated zero-turn activation check before handing host ownership back. There is no protected old interactive installation. Do not touch owner sessions or the shared tmux server.

## Interactive behavior

- Native Grok selects fresh, resumed, continued and forked conversations. Preserve native arguments in order; interpret wrapper-owned repeatable `-g`/`--group` comma lists, `-n`/`--name` (retain existing --peer-name compatibility) and --yolo alias anywhere before literal native `--`. Do not invent a native picker: retained 1.0.13 help says bare --resume means most recent. Pass native errors/exit/signal semantics through.
- Product-provided GROK_SESSION_ID is the helper's native identity. GROK_LEADER_SOCKET binds the launcher-owned private leader. Helpers remain per native live session: /new adds a peer and does not remove previous resident sessions.
- An initial name applies to the initially selected native session only. Later /new sessions keep their own native names. Rename must use native API and publish the actual resulting title under the same ID.
- The observer uses official ACP stdio, never session/load or session/prompt on a TUI-owned actor. Retain the quiet startup hold until an evidenced smaller native lifecycle replaces it. Do not read product storage as readiness.
- Ordinary Grok must not activate bus/tools. The native leader lacks per-launch plugin selection; ACTIVATION-AMENDMENT.md explicitly permits a globally registered but inert ordinary helper and discoverable generic skill. Managed activation is gated before owner/bus creation; installation and activation are distinct. Do not modify unrelated plugin/config entries or substitute native permission modes/automatic tool grants.

## Lane behavior

- One public Worker owns one native lane session and the existing shared Run/cursor. Open starts no model work. Exact offline load uses session/load and validates every returned identity shape; no native process --resume plus second load.
- Persistence is survival of owner death. Autoarchive is the independent post-native-terminal deadline. Idle-message stage/run is independent of both. Use the already merged shared policy/cursor, including notify target and explicit ack rules; do not invent private result survival or daemon-restart guarantees.
- Grok idle interject starts a native fallback turn. Never use it to implement idle stage. If no native non-query staging exists, a bounded FIFO of ONLY never-submitted text may retain local stage until the next owned Run. It lives with the worker, has no scheduler, timer or persistence promise, and is documented as local staging. Removing old Handoff means removing its submitted-message replay and native-token policy, not pretending Grok has native staging.
- Once a native write is attempted, do not requeue that input on timeout, EOF, cancellation or a terminal race. Refusal/uncertainty and native admission are separate. An actor-acknowledged delivery is never downgraded into automatic replay.
- A message-seeded run uses authenticated rendered input within the already reserved shared Run slot. ReportDelivery occurs only after evidenced native admission, outside reader/mutex and without waiting for model completion. Native prompt identity must bind output and terminal; thought chunks/foreign/replayed messages do not establish the run's answer.
- Native cancellation is a notification; its write success is not a terminal. Preserve matched native completed/failed/interrupted reasons and unavailable for missing terminal.

## Shared correctness and lifecycle

Use the accepted ServeSessionbus engine with a sole public Caller/Connection. Wait cancellation does not cancel work or consume output. Status/wait are nonconsuming; ack is explicit and ordered. Native policy may deny the tool. EOF, output failure, supersession and helper loss settle admitted work, close appropriate resources, and cannot resurrect presence. Distinguish matched/claimed completion from later cancellation.

Register correlations before native submission; bound pending routes, retained bytes and frame sizes. Drain late cancelled replies without replay. Reader dispatch cannot block on bus writes or result consumers. Preserve response-before-EOF and terminal-before-resource-retirement ordering. Failed Open owns its cleanup; completed Open native lifetime is independent of the Open call's context. Normal Close follows native close/EOF/drain/direct-exit behavior and does not invent transcript archival. Tests use controlled real RPC/process boundaries, not delays as success evidence.

## Selected native-composition decisions

A. Interactive activation: the actual native leader explicitly ignores --plugin-dir. ACTIVATION-AMENDMENT.md selects globally registered, inert ordinary MCP and exact private-leader activation for managed helpers. No native per-launch selection is claimed.

B. Initial naming: NAME-AMENDMENT.md selects one empty O_EXCL claim per unique actual launch, after native identity/leader validation. It names the first valid managed helper initialization once; native selected-ID comparison remains installed acceptance, and later /new helpers retain native naming.

C. Delivery admission/terminal crossing: DELIVERY-AMENDMENT.md binds primary running event43, actor acknowledgement57, native terminal280 and correlated RPC result283. Observer40/42/44 are not primary ordering authority. Primary fallback running297 and terminal439 support accounting a product-created continuation in the already reserved shared Run. No active-only native API is established, no fallback is inferred from cross-stream arrival timing, and submitted text is never replayed. The amendment carries exact correlation, replay filtering and completion requirements.

## Installed acceptance

Run on the same permanent candidate: ordinary/managed startup; exact initial name/mixed groups; native selector/rename/new/fork; public generic list and actual spawn/start/wait/ack/close; idle wake/active delivery with native receipt and independent transcript; stage then explicit work; shared message-run output/pointer/cancellation/ack; four persistence/autoarchive combinations; owner/helper death and normal close. Reuse already sealed native facts; only new composition/gap rows need new native work. Interactive regression follows lane changes on the same build. Publish scoped evidence and full source/dependency/binary/process footprint; all native gaps stay visible until resolved.
